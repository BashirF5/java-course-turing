package az.edu.turing.module01.lesson09;

import java.lang.Math;

public class Task2 {
    public static void main(String[] args) {
        int a, y, z, r, x, s;
        a = 5;
        y = 4 * a;
        z = a * a;
        r = 2;
        x = (int) (4 * Math.PI * r * r);
        s = (int) (Math.PI * r * r);
    }
}
